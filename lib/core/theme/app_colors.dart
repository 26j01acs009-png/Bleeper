import 'package:flutter/material.dart';

class AppColors {
  // ===== LIGHT MODE =====
  static const bg = Color(0xFFF9F9FB);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceAlt = Color(0xFFF2F2F7);

  static const textPrimary = Color(0xFF0A0A0A);
  static const textSecondary = Color(0xFF6E6E73);
  static const textTertiary = Color(0xFFAEAEB2);

  static const divider = Color(0xFFE5E5EA);

  static const accent = Color(0xFF007AFF);
  static const success = Color(0xFF34C759);
  static const warning = Color(0xFFFF9500);
  static const error = Color(0xFFFF3B30);

  // ===== DARK MODE =====
  static const darkBg = Color(0xFF0B0B0F);
  static const darkSurface = Color(0xFF16161A);
  static const darkSurfaceAlt = Color(0xFF1C1C20);

  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextSecondary = Color(0xFF8E8E93);
  static const darkTextTertiary = Color(0xFF636366);

  static const darkDivider = Color(0xFF2C2C2E);

  static const darkAccent = Color(0xFF0A84FF);
}
